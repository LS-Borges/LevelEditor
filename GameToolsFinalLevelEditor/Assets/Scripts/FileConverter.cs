using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FileConverter : MonoBehaviour
{
    // Grab a .txt file and converts it to a .bmp file. If the file is already .bmp, .png or .psd, return the file to the Map (LevelGenerator.cs)
    // 0 means nothing (white background), 1 is black(0,0,0), 2 is red(255,0,0), 3 is green(0,255,0), 4 is blue(0,0,255)

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
